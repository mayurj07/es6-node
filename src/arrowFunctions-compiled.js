"use strict";

/**
 * Created by mayujain on 1/4/17.
 */

var greeting = function greeting(message, name) {
    return message + name;
};

var arrowGreeting = function arrowGreeting(message, name) {
    return message + name;
};

var arrowExpression = function arrowExpression(message, name) {
    return message + name;
};

greeting("Hello ", "Mayur");
arrowExpression("Hello there ", "mayur");

//# sourceMappingURL=arrowFunctions-compiled.js.map